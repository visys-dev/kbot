replicaCount: 1

image:
  registry: "ghcr.io"
  repository: "visystemhub"
  tag: "v1.0.2"
  os: linux
  arch: amd64
  pullPolicy: IfNotPresent

TELE_TOKEN:
  secretName: "kbot"
  secretKey: "token"

resources: {}

securityContext:
  allowPrivilegeEscalation: false